import { SampledirDirective } from './sampledir.directive';

describe('SampledirDirective', () => {
  it('should create an instance', () => {
    const directive = new SampledirDirective();
    expect(directive).toBeTruthy();
  });
});
